{
  "name": "beryllium",
  "version": "1.0.0-dev",
  "description": "A simple CLI framework.",
  "main": "dist/application.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && npm test && tsc || echo \"Did not build: One or more tests did not pass\"",
    "format": "prettier --write src/*.ts",
    "lint": "eslint src/**",
    "test": "jest"
  },
  "author": "unwieldycat",
  "license": "WTFPL",
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^16.3.1",
    "@typescript-eslint/eslint-plugin": "^4.28.2",
    "@typescript-eslint/parser": "^4.28.2",
    "eslint": "^7.30.0",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "ts-jest": "^27.0.4"
  }
}
